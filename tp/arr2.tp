import lang

class A {
    value: int;

    fn __new__(this: *A, val: int) void {
        super.__new__();

        this.value = val;
    }
}

class B<T> {
    arr: A[]
    fn __new__(this: *B) void {
        super.__new__();

        this.arr = new A[2];
    }

    fn get_arr(this: *B) A[] {
        return NULL;
    }
}

fn main() int {
    bb := new B<A>();
    //bb.arr[0] = new A(34);
    ar := bb.get_arr()[0];

    return 0;
}