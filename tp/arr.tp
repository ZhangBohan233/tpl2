import lang;
import collections;

fn main() int {
    arr: int[] = new int[2];
    println(Array.class);
    obj := new List<List<List<Integer> > >();
    child := new List<List<Integer> >();
    grand := new List<Integer>();
    grand.append(new Integer(33));
    child.append(grand);
    obj.append(child);
    println(obj.get(0).get(0).get(0).intValue());
    b := 3;
    println(b < b < b >> 1);

    return arr.length;
}