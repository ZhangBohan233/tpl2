require {
    malloc
}

struct A<T> {
    a: int;
    b: T;
    add: fn(T) -> int;
}

fn A::add(x: T) int {

}

struct X {
    x: int;
}

fn main() int {
    b: *A<int[]> = new A<int[]>;
    b$a = 2;
    b$b = new int[4];
    (b$b)[0] = 1;
    (b$b)[1] = 2;
    return (b$b)[1];
}
