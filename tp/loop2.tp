import lang;
import collections;

fn main() int {
    lst := new List<Char>();
    for i := 0; i < 20; i++ {
        lst.append(new Char((i + 97) as char));
    }
    println(lst);
    return lst.size();
}